# Secure REST API with Spring Security and JWT

## 📋 Описание проекта

Это безопасный REST API, построенный на Spring Boot, который предоставляет функциональность аутентификации и управления пользователями. API защищен с использованием JWT (JSON Web Tokens) и включает меры защиты от распространенных уязвимостей веб-безопасности.

### 🛠 Технологический стек
- **Java 17+** с Spring Boot 3.x
- **Spring Security** для аутентификации и авторизации
- **JWT (jjwt)** для токен-базированной аутентификации
- **Spring Data JPA** для работы с базой данных
- **BCrypt** для хеширования паролей

## 🚀 Запуск проекта

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd secureapi
```

2. Настройте базу данных в `application.properties`:
```properties
spring.datasource.url=jdbc:postgresql://localhost:5432/secureapi
spring.datasource.username=postgres
spring.datasource.password=yourpassword
spring.jpa.hibernate.ddl-auto=update
```

3. Соберите и запустите приложение:
```bash
./mvnw clean install
./mvnw spring-boot:run
```

Приложение будет доступно по адресу: `http://localhost:8080`

## 📡 API Эндпоинты

### 🔐 Аутентификация

#### Вход в систему
```
POST /auth/login
Content-Type: application/json
```
**Тело запроса:**
```json
{
    "username": "your_username",
    "password": "your_password"
}
```

**Успешный ответ (200 OK):**
```json
{
    "token": "eyJhbGciOiJIUzI1NiJ9..."
}
```

### 👥 Управление пользователями

#### Получение всех пользователей
```
GET /api/data
Authorization: Bearer <your_jwt_token>
```
**Требуется:** Аутентификация (JWT токен)

**Ответ (200 OK):**
```json
[
    {
        "id": 1,
        "username": "user1",
        "email": "user1@example.com"
    }
]
```

#### Создание нового пользователя
```
POST /api/data
Authorization: Bearer <your_jwt_token>
Content-Type: application/json
```
**Тело запроса:**
```json
{
    "username": "newuser",
    "email": "newuser@example.com",
    "password": "securepassword"
}
```

**Успешный ответ (201 Created):**
```json
{
    "id": 2,
    "username": "newuser",
    "email": "newuser@example.com"
}
```

## 🛡️ Меры безопасности

### 1. 🔐 Защита от SQL-инъекций (SQLi)

**Реализация:**
- Использование **Spring Data JPA** с параметризованными запросами
- Автоматическое экранирование пользовательского ввода через JPA
- Repository-паттерн с типизированными методами (`findByUsername`, `findAll`)

**Пример из кода:**
```java
// UserRepository использует типизированные запросы
public interface UserRepository extends JpaRepository<User, Long> {
    Optional<User> findByUsername(String username);
}
```

### 2. 🛡️ Защита от XSS (Cross-Site Scripting)

**Реализация:**
- Автоматическое экранирование HTML в контроллерах
- Использование `HtmlUtils.htmlEscape()` для пользовательского ввода

**Пример из кода:**
```java
@PostMapping
public ResponseEntity<User> createUser(@RequestBody User user) {
    // Экранирование пользовательского ввода перед сохранением
    user.setUsername(HtmlUtils.htmlEscape(user.getUsername()));
    user.setEmail(HtmlUtils.htmlEscape(user.getEmail()));
    // ...
}
```

### 3. 🔑 Аутентификация и авторизация

#### JWT-базированная аутентификация:
- **Генерация токенов:** Использование HMAC-SHA256 для подписи токенов
- **Валидация токенов:** Проверка подписи и срока действия
- **Защищенные ключи:** Динамическая генерация или конфигурация секретных ключей

**Конфигурация JWT:**
```java
// JwtUtil - утилита для работы с JWT токенами
private static final Key KEY = Keys.secretKeyFor(SignatureAlgorithm.HS256);
private static final long EXPIRATION = 1000 * 60 * 60; // 1 час
```

#### Spring Security конфигурация:
- **Фильтр JWT:** `JwtFilter` проверяет токен в каждом запросе
- **Защита эндпоинтов:** Только аутентифицированные пользователи имеют доступ к `/api/**`
- **Открытые эндпоинты:** `/auth/**` доступны без аутентификации

### 4. 🔒 Шифрование паролей

**Реализация:**
- Использование **BCrypt** для хеширования паролей
- Автоматическое добавление "соли" (salt) к каждому паролю

**Конфигурация:**
```java
@Bean
public PasswordEncoder passwordEncoder() {
    return new BCryptPasswordEncoder();
}
```

### 5. 📝 Безопасность сессии

- **Stateless аутентификация:** Не используются сессии на сервере
- **JWT токены:** Все состояние аутентификации хранится на клиенте
```
